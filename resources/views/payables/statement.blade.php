<x-app-layout :title="$supplier->name.' Statement'"><x-page-header :title="$supplier->name.' Statement'" :description="'As of '.$asOf->format('M d, Y')" /><div class="mb-5 flex gap-3 text-sm"><a href="{{ route('payables.index', ['as_of'=>$asOf->toDateString(),'supplier_id'=>$supplier->id]) }}">Back to aging</a><a href="{{ route('supplier-statements.print', [$supplier,'as_of'=>$asOf->toDateString()]) }}">Print</a></div><div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><h2 class="font-semibold">Invoices</h2>@foreach($invoices as $row)<div class="flex justify-between border-b py-3"><span>{{ $row['invoice']->supplier_invoice_number }} · due {{ $row['invoice']->due_date->toDateString() }}</span><strong>₱{{ number_format((float)$row['balance'],2) }}</strong></div>@endforeach<h2 class="mt-6 font-semibold">Unapplied advances</h2>@foreach($advances as $row)<div class="flex justify-between border-b py-3"><span>{{ $row['payment']->payment_number }}</span><strong>₱{{ number_format((float)$row['unapplied'],2) }}</strong></div>@endforeach<div class="mt-6 flex justify-between text-lg"><strong>Net payable</strong><strong>₱{{ number_format((float)$balance,2) }}</strong></div></div></x-app-layout>

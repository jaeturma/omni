<x-app-layout title="Expenses Payable"><x-page-header title="Expenses Payable" description="Approved or reimbursable operating expenses awaiting payment." /><x-payable-nav :filters="$filters" /><x-payable-filters :filters="$filters" :suppliers="$suppliers" :action="route('payables.expenses')" /><div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200">@forelse($rows as $row)<div class="flex justify-between border-b py-3"><span>{{ $row['expense']->expense_number }} · {{ $row['expense']->supplier?->name ?? $row['expense']->payee_name }}</span><strong>₱{{ number_format((float)$row['payable'],2) }}</strong></div>@empty<p>No expenses payable found.</p>@endforelse</div><div class="mt-5">{{ $rows->links() }}</div></x-app-layout>

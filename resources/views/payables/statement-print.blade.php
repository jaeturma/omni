<!doctype html><html><head><title>{{ $supplier->name }} Statement</title><style>body{font-family:sans-serif}.row{display:flex;justify-content:space-between;border-bottom:1px solid #bbb;padding:7px}</style></head><body><h1>{{ $supplier->name }} Statement</h1><p>As of {{ $asOf->format('M d, Y') }}</p><h2>Invoices</h2>@foreach($invoices as $row)<div class="row"><span>{{ $row['invoice']->supplier_invoice_number }}</span><span>₱{{ number_format((float)$row['balance'],2) }}</span></div>@endforeach<h2>Unapplied advances</h2>@foreach($advances as $row)<div class="row"><span>{{ $row['payment']->payment_number }}</span><span>₱{{ number_format((float)$row['unapplied'],2) }}</span></div>@endforeach<h2>Net payable: ₱{{ number_format((float)$balance,2) }}</h2></body></html>

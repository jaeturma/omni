<x-app-layout title="Banks"><x-page-header title="Banks" description="Maintain bank institution master records without accounts or balances." /><div class="mb-5 flex flex-wrap items-end justify-between gap-4"><form method="GET" action="{{ route('banks.index') }}" class="flex flex-wrap items-end gap-3"><label class="flex flex-col gap-1 text-sm font-medium">Search<input name="search" value="{{ request('search') }}" placeholder="Code, name, or SWIFT" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Status<select name="status" class="rounded-lg border border-slate-300 px-3 py-2"><option value="">All</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select></label><button class="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold">Filter</button></form>@can('create',\App\Models\Bank::class)<a href="{{ route('banks.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create bank</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-6 py-3">Code</th><th class="px-6 py-3">Bank</th><th class="px-6 py-3">SWIFT/BIC</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($banks as $bank)<tr><td class="px-6 py-4 font-mono font-medium">{{ $bank->code }}</td><td class="px-6 py-4 font-medium">{{ $bank->name }}</td><td class="px-6 py-4 font-mono">{{ $bank->swift_code ?: '—' }}</td><td class="px-6 py-4 capitalize">{{ $bank->status }}</td><td class="px-6 py-4">@can('update',$bank)<a href="{{ route('banks.edit',$bank) }}" class="font-semibold text-blue-700">Edit</a>@endcan</td></tr>@empty<tr><td colspan="5" class="px-6 py-8 text-center text-slate-500">No banks found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $banks->links() }}</div></x-app-layout>

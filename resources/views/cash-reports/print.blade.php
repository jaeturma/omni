<!DOCTYPE html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Cash Position Report</title>@vite(['resources/css/app.css'])</head>
<body class="bg-white p-8 text-slate-950">
    <h1 class="text-2xl font-bold">Cash Position Report</h1><p class="text-sm">{{ $filters['start_date'] }} to {{ $filters['end_date'] }} · As of {{ $filters['as_of'] }}</p>
    <div class="mt-5 grid grid-cols-3 gap-4"><div>Receipts: PHP {{ number_format((float) $report['receipts'], 2) }}</div><div>Disbursements: PHP {{ number_format((float) $report['disbursements'], 2) }}</div><div>Net: PHP {{ number_format((float) $report['net_movement'], 2) }}</div></div>
    <h2 class="mt-6 text-lg font-semibold">Account balances</h2><table class="mt-2 min-w-full text-sm"><thead><tr><th class="text-left">Account</th><th>Opening</th><th>Movement</th><th>Closing</th><th>As of</th></tr></thead><tbody>@foreach($report['positions'] as $position)<tr class="border-t"><td>{{ $position['account']->code }}</td><td class="text-right">{{ $position['opening'] }}</td><td class="text-right">{{ $position['movement'] }}</td><td class="text-right">{{ $position['closing'] }}</td><td class="text-right">{{ $position['as_of'] }}</td></tr>@endforeach</tbody></table>
    <h2 class="mt-6 text-lg font-semibold">Posted activity</h2><table class="mt-2 min-w-full text-sm"><thead><tr><th>Date</th><th>Account</th><th>Type</th><th>Reference</th><th>Amount</th></tr></thead><tbody>@foreach($activity as $transaction)<tr class="border-t"><td>{{ $transaction->transaction_date->toDateString() }}</td><td>{{ $transaction->financialAccount->code }}</td><td>{{ str($transaction->type->value)->headline() }}</td><td>{{ $transaction->reference_number }}</td><td class="text-right">{{ $transaction->amount }}</td></tr>@endforeach</tbody></table>
</body></html>

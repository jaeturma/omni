<x-app-layout title="Supplier payment"><div class="mb-6 flex flex-wrap items-start justify-between gap-4"><x-page-header title="{{ $supplierPayment->payment_number ?? 'Draft #'.$supplierPayment->id }}" description="{{ ucfirst(str_replace('_', ' ', $supplierPayment->status->value)) }} payment to {{ $supplierPayment->supplier->name }}" /><div class="flex gap-2">@can('update', $supplierPayment)<a href="{{ route('supplier-payments.edit', $supplierPayment) }}" class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Edit</a>@endcan @can('print', $supplierPayment)<a href="{{ route('supplier-payments.print', $supplierPayment) }}" class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Print</a>@endcan</div></div><section class="grid gap-4 rounded-2xl bg-white p-6 text-sm shadow-sm ring-1 ring-slate-200 md:grid-cols-4"><div><span class="text-slate-500">Payment date</span><p>{{ $supplierPayment->payment_date->format('M d, Y') }}</p></div><div><span class="text-slate-500">Method / Bank</span><p>{{ $supplierPayment->paymentMethod->name }} / {{ $supplierPayment->bank?->name ?? '—' }}</p></div><div><span class="text-slate-500">Reference</span><p>{{ $supplierPayment->reference_number ?: '—' }}</p></div><div><span class="text-slate-500">Settlement / Unapplied</span><p>{{ $supplierPayment->gross_settlement_amount }} / {{ $supplierPayment->unapplied_amount }}</p></div><div><span class="text-slate-500">Cash paid</span><p>{{ $supplierPayment->net_cash_paid }}</p></div><div><span class="text-slate-500">Withholding</span><p>{{ $supplierPayment->withholding_amount }}</p></div><div><span class="text-slate-500">Other deductions</span><p>{{ $supplierPayment->other_deductions }}</p></div></section><section class="mt-5 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 class="mb-4 font-semibold">Allocations</h2><div class="flex flex-col gap-2">@forelse($supplierPayment->allocations as $allocation)<div class="flex justify-between gap-4 text-sm"><span>{{ $allocation->supplierInvoice->internal_number }} · {{ $allocation->status->value }}</span><span>{{ $allocation->amount }}</span></div>@empty<p class="text-sm text-slate-500">No allocations.</p>@endforelse</div></section>@can('allocate', $supplierPayment)@if($openInvoices->isNotEmpty())<form method="POST" action="{{ route('supplier-payments.allocate', $supplierPayment) }}" class="mt-5 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">@csrf<h2 class="mb-4 font-semibold">Allocate payment</h2><div class="flex flex-col gap-3">@foreach($openInvoices as $index => $invoice)<div class="grid items-end gap-3 md:grid-cols-3"><input type="hidden" name="allocations[{{ $index }}][supplier_invoice_id]" value="{{ $invoice->id }}"><div class="text-sm"><p class="font-medium">{{ $invoice->internal_number }}</p><p class="text-slate-500">Balance {{ $invoice->balance_due }}</p></div><label class="flex flex-col gap-1 text-sm">Amount<input type="number" min="0" max="{{ $invoice->balance_due }}" step="0.0001" name="allocations[{{ $index }}][amount]" value="0.0000" class="rounded-lg border border-slate-300 px-3 py-2"></label></div>@endforeach</div><button class="mt-4 rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Apply allocations</button></form>@endif @endcan<div class="mt-5 flex flex-wrap gap-3">@can('post', $supplierPayment)<form method="POST" action="{{ route('supplier-payments.transition', $supplierPayment) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="posted"><button class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Post payment</button></form>@endcan @can('void', $supplierPayment)<form method="POST" action="{{ route('supplier-payments.transition', $supplierPayment) }}" class="flex gap-2">@csrf @method('PATCH')<input type="hidden" name="status" value="voided"><input name="reason" required placeholder="Void reason" class="rounded-lg border border-slate-300 px-3 py-2"><button class="rounded-lg border border-red-300 px-4 py-2 text-sm font-semibold text-red-700">Void and reverse</button></form>@endcan</div></x-app-layout>

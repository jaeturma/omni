@php($editing = isset($supplierPayment))
<x-app-layout title="{{ $editing ? 'Edit' : 'Create' }} supplier payment"><x-page-header title="{{ $editing ? 'Edit supplier payment draft' : 'Create supplier payment' }}" description="Gross settlement must equal cash paid plus withholding and other deductions." /><form method="POST" action="{{ $editing ? route('supplier-payments.update', $supplierPayment) : route('supplier-payments.store') }}" class="flex flex-col gap-5">@csrf @if($editing)@method('PUT')@endif<section class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-2"><label class="flex flex-col gap-1 text-sm font-medium">Supplier<select name="supplier_id" required class="rounded-lg border border-slate-300 px-3 py-2">@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}" @selected(old('supplier_id', $supplierPayment->supplier_id ?? null) == $supplier->id)>{{ $supplier->name }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Payment method<select name="payment_method_id" required class="rounded-lg border border-slate-300 px-3 py-2">@foreach($paymentMethods as $method)<option value="{{ $method->id }}" @selected(old('payment_method_id', $supplierPayment->payment_method_id ?? null) == $method->id)>{{ $method->name }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Bank (optional)<select name="bank_id" class="rounded-lg border border-slate-300 px-3 py-2"><option value="">None</option>@foreach($banks as $bank)<option value="{{ $bank->id }}" @selected(old('bank_id', $supplierPayment->bank_id ?? null) == $bank->id)>{{ $bank->name }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Cheque / transfer reference<input name="reference_number" value="{{ old('reference_number', $supplierPayment->reference_number ?? '') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Payment date<input type="date" name="payment_date" value="{{ old('payment_date', isset($supplierPayment) ? $supplierPayment->payment_date->toDateString() : now()->toDateString()) }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label></section><section class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-4">@foreach(['gross_settlement_amount' => 'Gross settlement', 'withholding_amount' => 'Withholding', 'other_deductions' => 'Other deductions', 'net_cash_paid' => 'Net cash paid'] as $field => $label)<label class="flex flex-col gap-1 text-sm font-medium">{{ $label }}<input type="number" min="0" step="0.0001" name="{{ $field }}" value="{{ old($field, $supplierPayment->{$field} ?? '0.0000') }}" required class="rounded-lg border border-slate-300 px-3 py-2">@error($field)<span class="text-red-700">{{ $message }}</span>@enderror</label>@endforeach</section><label class="flex flex-col gap-1 rounded-2xl bg-white p-6 text-sm font-medium shadow-sm ring-1 ring-slate-200">Notes<textarea name="notes" rows="3" class="rounded-lg border border-slate-300 px-3 py-2">{{ old('notes', $supplierPayment->notes ?? '') }}</textarea></label><button class="self-start rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Save payment draft</button></form></x-app-layout>

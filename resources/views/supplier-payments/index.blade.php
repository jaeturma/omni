<x-app-layout title="Supplier payments"><div class="mb-6 flex flex-wrap items-start justify-between gap-4"><x-page-header title="Supplier payments" description="Record supplier settlements, deductions, and unapplied advances." />@can('create', \App\Models\SupplierPayment::class)<a href="{{ route('supplier-payments.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">New payment</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead><tr><th class="px-5 py-3 text-left">Number</th><th class="px-5 py-3 text-left">Supplier</th><th class="px-5 py-3 text-left">Date / Method</th><th class="px-5 py-3 text-right">Settlement</th><th class="px-5 py-3 text-right">Unapplied</th><th class="px-5 py-3 text-left">Status</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td class="px-5 py-4"><a href="{{ route('supplier-payments.show', $payment) }}" class="font-semibold text-blue-700">{{ $payment->payment_number ?? 'Draft #'.$payment->id }}</a></td><td class="px-5 py-4">{{ $payment->supplier->name }}</td><td class="px-5 py-4">{{ $payment->payment_date->format('M d, Y') }} · {{ $payment->paymentMethod->name }}</td><td class="px-5 py-4 text-right">{{ $payment->gross_settlement_amount }}</td><td class="px-5 py-4 text-right">{{ $payment->unapplied_amount }}</td><td class="px-5 py-4 capitalize">{{ str_replace('_', ' ', $payment->status->value) }}</td></tr>@empty<tr><td colspan="6" class="px-5 py-8 text-center text-slate-500">No supplier payments found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $payments->links() }}</div></x-app-layout>

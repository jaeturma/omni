<!doctype html><html lang="en"><head><meta charset="utf-8"><title>{{ $payment->payment_number ?? 'Draft payment' }}</title></head><body><h1>Supplier Payment {{ $payment->payment_number ?? 'Draft' }}</h1><p>{{ $payment->supplier->name }} · {{ $payment->payment_date->toDateString() }}</p><p>Gross settlement {{ $payment->gross_settlement_amount }} · Cash paid {{ $payment->net_cash_paid }} · Withholding {{ $payment->withholding_amount }} · Other deductions {{ $payment->other_deductions }} · Unapplied {{ $payment->unapplied_amount }}</p><h2>Allocations</h2><ul>@foreach($payment->allocations as $allocation)<li>{{ $allocation->supplierInvoice->internal_number }} — {{ $allocation->amount }} ({{ $allocation->status->value }})</li>@endforeach</ul></body></html>

<x-app-layout title="Create User"><x-page-header title="Create User" description="Create an application account and assign access roles." />
<form method="POST" action="{{ route('users.store') }}" class="max-w-2xl rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">@csrf
<div class="grid gap-4 md:grid-cols-2"><label class="flex flex-col gap-1 text-sm font-medium">Name<input name="name" value="{{ old('name') }}" required class="rounded-lg border border-slate-300 px-3 py-2">@error('name')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium">Email<input type="email" name="email" value="{{ old('email') }}" required class="rounded-lg border border-slate-300 px-3 py-2">@error('email')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium">Password<input type="password" name="password" required class="rounded-lg border border-slate-300 px-3 py-2">@error('password')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium">Confirm password<input type="password" name="password_confirmation" required class="rounded-lg border border-slate-300 px-3 py-2"></label></div>
<fieldset class="mt-5"><legend class="text-sm font-semibold">Roles</legend><div class="mt-2 flex flex-wrap gap-4">@foreach ($roles as $role)<label class="flex items-center gap-2 text-sm"><input type="checkbox" name="roles[]" value="{{ $role->name }}" @checked(in_array($role->name, old('roles', []), true))> {{ $role->name }}</label>@endforeach</div>@error('roles')<p class="mt-1 text-sm text-red-700">{{ $message }}</p>@enderror</fieldset><label class="mt-5 flex items-center gap-2 text-sm"><input type="hidden" name="active" value="0"><input type="checkbox" name="active" value="1" @checked(old('active', true))> Active</label><button class="mt-5 rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create user</button></form></x-app-layout>

<x-app-layout title="Edit User"><x-page-header title="Edit User" description="Update account details, roles, password, and status." />
<form method="POST" action="{{ route('users.update', $managedUser) }}" class="max-w-2xl rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">@csrf @method('PUT')
<div class="grid gap-4 md:grid-cols-2"><label class="flex flex-col gap-1 text-sm font-medium">Name<input name="name" value="{{ old('name', $managedUser->name) }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Email<input type="email" name="email" value="{{ old('email', $managedUser->email) }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">New password <span class="font-normal text-slate-500">(leave blank to retain)</span><input type="password" name="password" class="rounded-lg border border-slate-300 px-3 py-2">@error('password')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium">Confirm new password<input type="password" name="password_confirmation" class="rounded-lg border border-slate-300 px-3 py-2"></label></div>
@php($selectedRoles = old('roles', $managedUser->roles->pluck('name')->all()))<fieldset class="mt-5"><legend class="text-sm font-semibold">Roles</legend><div class="mt-2 flex flex-wrap gap-4">@foreach ($roles as $role)<label class="flex items-center gap-2 text-sm"><input type="checkbox" name="roles[]" value="{{ $role->name }}" @checked(in_array($role->name, $selectedRoles, true))> {{ $role->name }}</label>@endforeach</div>@error('roles')<p class="mt-1 text-sm text-red-700">{{ $message }}</p>@enderror</fieldset><label class="mt-5 flex items-center gap-2 text-sm"><input type="hidden" name="active" value="0"><input type="checkbox" name="active" value="1" @checked(old('active', $managedUser->active))> Active</label><button class="mt-5 rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Save user</button></form></x-app-layout>

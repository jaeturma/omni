<x-app-layout title="Users"><x-page-header title="User Administration" description="Manage application access, roles, and account status." />
<div class="mb-5 flex flex-wrap gap-3"><a href="{{ route('users.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create user</a><a href="{{ route('roles.index') }}" class="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold">Role-permission matrix</a></div>
<div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-slate-600"><tr><th class="px-6 py-3">User</th><th class="px-6 py-3">Roles</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($users as $user)<tr><td class="px-6 py-4"><p class="font-medium">{{ $user->name }}</p><p class="text-slate-500">{{ $user->email }}</p></td><td class="px-6 py-4">{{ $user->roles->pluck('name')->join(', ') }}</td><td class="px-6 py-4">{{ $user->active ? 'Active' : 'Inactive' }}</td><td class="px-6 py-4"><a href="{{ route('users.edit', $user) }}" class="font-semibold text-blue-700">Edit</a></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $users->links() }}</div></x-app-layout>

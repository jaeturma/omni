<x-app-layout title="Quotations"><x-page-header title="Quotations" description="Create and manage customer quotations without receivable or stock effects." /><div class="mb-5 flex flex-wrap items-end justify-between gap-4"><form method="GET" class="flex flex-wrap items-end gap-3"><label class="flex flex-col gap-1 text-sm font-medium">Search<input name="search" value="{{ request('search') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Status<select name="status" class="rounded-lg border border-slate-300 px-3 py-2"><option value="">All</option>@foreach(\App\Enums\QuotationStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select></label><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Filter</button></form>@can('create',\App\Models\Quotation::class)<a href="{{ route('quotations.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create quotation</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left"><tr><th class="px-6 py-3">Number</th><th class="px-6 py-3">Date</th><th class="px-6 py-3">Customer</th><th class="px-6 py-3">Total</th><th class="px-6 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($quotations as $quotation)<tr><td class="px-6 py-4"><a href="{{ route('quotations.show',$quotation) }}" class="font-semibold text-blue-700">{{ $quotation->quotation_number ?? 'Draft #'.$quotation->id }}</a></td><td class="px-6 py-4">{{ $quotation->quotation_date->format('M d, Y') }}</td><td class="px-6 py-4">{{ $quotation->customer_name }}</td><td class="px-6 py-4">₱{{ number_format((float)$quotation->grand_total,2) }}</td><td class="px-6 py-4 capitalize">{{ str_replace('_',' ',$quotation->status->value) }}</td></tr>@empty<tr><td colspan="5" class="px-6 py-8 text-center text-slate-500">No quotations found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $quotations->links() }}</div></x-app-layout>

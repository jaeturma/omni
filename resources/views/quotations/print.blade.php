<!DOCTYPE html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>{{ $quotation->quotation_number ?? 'Draft Quotation' }}</title>@vite(['resources/css/app.css'])</head><body class="bg-white p-8 text-slate-950"><main class="mx-auto max-w-4xl"><header class="mb-8 border-b pb-5"><h1 class="text-3xl font-bold">Quotation</h1><p>{{ $quotation->quotation_number ?? 'DRAFT' }} · {{ $quotation->quotation_date->format('F d, Y') }}</p></header><div class="grid grid-cols-2 gap-8 text-sm"><div><h2 class="font-bold">Customer</h2><p>{{ $quotation->customer_name }}</p><p class="whitespace-pre-line">{{ $quotation->billing_address }}</p></div><div><h2 class="font-bold">Valid until</h2><p>{{ $quotation->valid_until->format('F d, Y') }}</p><p>{{ $quotation->reference }}</p></div></div><table class="mt-8 w-full text-sm"><thead class="border-b"><tr><th class="py-2 text-left">Description</th><th class="text-right">Qty</th><th class="text-right">Unit price</th><th class="text-right">Amount</th></tr></thead><tbody>@foreach($quotation->lines as $line)<tr class="border-b"><td class="py-3">{{ $line->description }} ({{ $line->uom_code }})</td><td class="text-right">{{ $line->quantity }}</td><td class="text-right">₱{{ number_format((float)$line->unit_price,2) }}</td><td class="text-right">₱{{ number_format((float)$line->net_amount,2) }}</td></tr>@endforeach</tbody></table><div class="ml-auto mt-5 w-72 text-sm"><p class="flex justify-between font-bold"><span>Grand total</span><span>₱{{ number_format((float)$quotation->grand_total,2) }}</span></p></div><section class="mt-8 whitespace-pre-line text-sm"><h2 class="font-bold">Terms and conditions</h2>{{ $quotation->terms_and_conditions }}</section></main></body></html>

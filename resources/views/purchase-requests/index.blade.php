<x-app-layout title="Purchase requests"><x-page-header title="Purchase requests" description="Manage internal purchasing requirements and supplier canvass." /><div class="mb-5 flex flex-wrap items-end justify-between gap-4"><form method="GET" class="flex flex-wrap items-end gap-3"><label class="flex flex-col gap-1 text-sm font-medium">Search<input name="search" value="{{ request('search') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Status<select name="status" class="rounded-lg border border-slate-300 px-3 py-2"><option value="">All</option>@foreach(\App\Enums\PurchaseRequestStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select></label><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Filter</button></form>@can('create', \App\Models\PurchaseRequest::class)<a href="{{ route('purchase-requests.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create request</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left"><tr><th class="px-6 py-3">Number</th><th class="px-6 py-3">Date</th><th class="px-6 py-3">Requester</th><th class="px-6 py-3">Purpose</th><th class="px-6 py-3 text-right">Estimate</th><th class="px-6 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($purchaseRequests as $request)<tr><td class="px-6 py-4"><a href="{{ route('purchase-requests.show', $request) }}" class="font-semibold text-blue-700">{{ $request->request_number ?? 'Draft #'.$request->id }}</a></td><td class="px-6 py-4">{{ $request->request_date->format('M d, Y') }}</td><td class="px-6 py-4">{{ $request->requester->name }}</td><td class="max-w-sm truncate px-6 py-4">{{ $request->purpose }}</td><td class="px-6 py-4 text-right">₱{{ number_format((float)$request->estimated_total, 2) }}</td><td class="px-6 py-4 capitalize">{{ $request->status->value }}</td></tr>@empty<tr><td colspan="6" class="px-6 py-8 text-center text-slate-500">No purchase requests found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $purchaseRequests->links() }}</div></x-app-layout>

<x-app-layout title="Edit supplier invoice"><x-page-header title="Edit supplier invoice draft" description="Posted supplier invoices cannot be edited." /><form method="POST" action="{{ route('supplier-invoices.update', $invoice) }}" class="flex flex-col gap-5">@csrf @method('PUT')<section class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-2"><label class="flex flex-col gap-1 text-sm font-medium">Supplier<select name="supplier_id" class="rounded-lg border border-slate-300 px-3 py-2">@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}" @selected($invoice->supplier_id === $supplier->id)>{{ $supplier->name }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Period<select name="fiscal_period_id" class="rounded-lg border border-slate-300 px-3 py-2">@foreach($periods as $period)<option value="{{ $period->id }}" @selected($invoice->fiscal_period_id === $period->id)>{{ $period->name }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Supplier invoice number<input name="supplier_invoice_number" value="{{ old('supplier_invoice_number', $invoice->supplier_invoice_number) }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Invoice date<input type="date" name="invoice_date" value="{{ $invoice->invoice_date->toDateString() }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Due date<input type="date" name="due_date" value="{{ $invoice->due_date->toDateString() }}" class="rounded-lg border border-slate-300 px-3 py-2"></label></section><section class="flex flex-col gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">@foreach($invoice->lines as $index => $line)<div class="grid gap-3 md:grid-cols-4"><input type="hidden" name="lines[{{ $index }}][purchase_order_line_id]" value="{{ $line->purchase_order_line_id }}"><input type="hidden" name="lines[{{ $index }}][receiving_record_line_id]" value="{{ $line->receiving_record_line_id }}"><input type="hidden" name="lines[{{ $index }}][item_type]" value="{{ $line->item_type }}"><input type="hidden" name="lines[{{ $index }}][sku]" value="{{ $line->sku }}"><input type="hidden" name="lines[{{ $index }}][uom_code]" value="{{ $line->uom_code }}"><input type="hidden" name="lines[{{ $index }}][uom_name]" value="{{ $line->uom_name }}"><label class="flex flex-col gap-1 text-sm">Description<input name="lines[{{ $index }}][description]" value="{{ $line->description }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm">Quantity<input name="lines[{{ $index }}][quantity]" value="{{ $line->quantity }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm">Unit cost<input name="lines[{{ $index }}][unit_cost]" value="{{ $line->unit_cost }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm">Discount %<input name="lines[{{ $index }}][discount_rate]" value="{{ $line->discount_rate }}" class="rounded-lg border border-slate-300 px-3 py-2"></label></div>@endforeach</section>@foreach(['freight_amount','other_charges_amount','withholding_expected_amount'] as $field)<input type="hidden" name="{{ $field }}" value="{{ $invoice->{$field} }}">@endforeach<textarea name="notes" class="rounded-lg border border-slate-300 px-3 py-2">{{ $invoice->notes }}</textarea><button class="self-start rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Update draft</button></form></x-app-layout>

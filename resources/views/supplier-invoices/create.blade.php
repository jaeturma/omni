<x-app-layout title="Create supplier invoice">
    <x-page-header title="Create supplier invoice" description="Enter a direct service/expense invoice or bill an accepted receiving record." />
    <form method="POST" action="{{ route('supplier-invoices.store') }}" class="flex flex-col gap-5">@csrf
        <section class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-2"><label class="flex flex-col gap-1 text-sm font-medium">Supplier<select name="supplier_id" required class="rounded-lg border border-slate-300 px-3 py-2">@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}" @selected(old('supplier_id', $receivingRecord?->supplier_id) == $supplier->id)>{{ $supplier->name }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Fiscal period<select name="fiscal_period_id" required class="rounded-lg border border-slate-300 px-3 py-2">@foreach($periods as $period)<option value="{{ $period->id }}">{{ $period->name }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Supplier invoice number<input name="supplier_invoice_number" value="{{ old('supplier_invoice_number') }}" required class="rounded-lg border border-slate-300 px-3 py-2">@error('supplier_invoice_number')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium">Invoice date<input type="date" name="invoice_date" value="{{ old('invoice_date', now()->toDateString()) }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Due date<input type="date" name="due_date" value="{{ old('due_date', now()->toDateString()) }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label>@if($receivingRecord)<input type="hidden" name="purchase_order_id" value="{{ $receivingRecord->purchase_order_id }}"><input type="hidden" name="receiving_record_id" value="{{ $receivingRecord->id }}"><div class="rounded-lg bg-blue-50 p-3 text-sm text-blue-900">Source: {{ $receivingRecord->receiving_number }}</div>@endif</section>
        <section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 class="mb-4 font-semibold">Invoice lines</h2><div class="flex flex-col gap-4">@php($sourceLines = $receivingRecord?->lines ?? collect([null]))@foreach($sourceLines as $index => $sourceLine)<div class="grid gap-3 rounded-xl border border-slate-200 p-4 md:grid-cols-6">@if($sourceLine)<input type="hidden" name="lines[{{ $index }}][receiving_record_line_id]" value="{{ $sourceLine->id }}"><input type="hidden" name="lines[{{ $index }}][purchase_order_line_id]" value="{{ $sourceLine->purchase_order_line_id }}">@endif<label class="flex flex-col gap-1 text-sm font-medium md:col-span-2">Description<input name="lines[{{ $index }}][description]" value="{{ old("lines.$index.description", $sourceLine?->description) }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Type<select name="lines[{{ $index }}][item_type]" class="rounded-lg border border-slate-300 px-3 py-2">@foreach(['service', 'expense', 'product'] as $type)<option value="{{ $type }}" @selected(old("lines.$index.item_type", $sourceLine?->item_type ?? 'service') === $type)>{{ ucfirst($type) }}</option>@endforeach</select></label><label class="flex flex-col gap-1 text-sm font-medium">Quantity<input type="number" min="0" step="0.0001" name="lines[{{ $index }}][quantity]" value="{{ old("lines.$index.quantity", $sourceLine?->accepted_quantity ?? '1.0000') }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Unit cost<input type="number" min="0" step="0.0001" name="lines[{{ $index }}][unit_cost]" value="{{ old("lines.$index.unit_cost", $sourceLine?->purchaseOrderLine?->unit_cost ?? '0.0000') }}" required class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Discount %<input type="number" min="0" max="100" step="0.000001" name="lines[{{ $index }}][discount_rate]" value="{{ old("lines.$index.discount_rate", $sourceLine?->purchaseOrderLine?->discount_rate ?? '0') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><input type="hidden" name="lines[{{ $index }}][sku]" value="{{ $sourceLine?->sku }}"><input type="hidden" name="lines[{{ $index }}][uom_code]" value="{{ $sourceLine?->uom_code ?? 'LOT' }}"><input type="hidden" name="lines[{{ $index }}][uom_name]" value="{{ $sourceLine?->uom_name ?? 'Lot' }}"></div>@endforeach</div>@error('lines')<p class="mt-2 text-sm text-red-700">{{ $message }}</p>@enderror</section>
        <section class="grid gap-4 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 md:grid-cols-3">@foreach(['freight_amount' => 'Freight', 'other_charges_amount' => 'Other charges', 'withholding_expected_amount' => 'Expected withholding'] as $field => $label)<label class="flex flex-col gap-1 text-sm font-medium">{{ $label }}<input type="number" min="0" step="0.0001" name="{{ $field }}" value="{{ old($field, '0.0000') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label>@endforeach</section><label class="flex flex-col gap-1 rounded-2xl bg-white p-6 text-sm font-medium shadow-sm ring-1 ring-slate-200">Notes<textarea name="notes" rows="3" class="rounded-lg border border-slate-300 px-3 py-2">{{ old('notes') }}</textarea></label><div class="flex gap-3"><button class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Save draft</button><a href="{{ route('supplier-invoices.index') }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Cancel</a></div>
    </form>
</x-app-layout>

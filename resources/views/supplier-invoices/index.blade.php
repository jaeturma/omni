<x-app-layout title="Supplier invoices">
    <div class="mb-6 flex flex-wrap items-start justify-between gap-4"><x-page-header title="Supplier invoices" description="Operational supplier payables without ledger or tax-return posting." />@can('create', \App\Models\SupplierInvoice::class)<a href="{{ route('supplier-invoices.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">New supplier invoice</a>@endcan</div>
    <form method="GET" class="mb-5 flex flex-wrap items-end gap-3"><label class="flex flex-col gap-1 text-sm font-medium">Search<input name="search" value="{{ request('search') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Status<select name="status" class="rounded-lg border border-slate-300 px-3 py-2"><option value="">All</option>@foreach(\App\Enums\SupplierInvoiceStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ ucfirst(str_replace('_', ' ', $status->value)) }}</option>@endforeach</select></label><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Filter</button></form>
    <div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left"><tr><th class="px-5 py-3">Internal / Supplier no.</th><th class="px-5 py-3">Supplier</th><th class="px-5 py-3">Due</th><th class="px-5 py-3 text-right">Balance</th><th class="px-5 py-3">Status</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td class="px-5 py-4"><a class="font-semibold text-blue-700" href="{{ route('supplier-invoices.show', $invoice) }}">{{ $invoice->internal_number ?? 'Draft #'.$invoice->id }}</a><p class="text-xs text-slate-500">{{ $invoice->supplier_invoice_number }}</p></td><td class="px-5 py-4">{{ $invoice->supplier_name }}</td><td class="px-5 py-4">{{ $invoice->due_date->format('M d, Y') }}</td><td class="px-5 py-4 text-right">₱{{ number_format((float) $invoice->balance_due, 2) }}</td><td class="px-5 py-4 capitalize">{{ str_replace('_', ' ', $invoice->status->value) }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-8 text-center text-slate-500">No supplier invoices found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $invoices->links() }}</div>
</x-app-layout>

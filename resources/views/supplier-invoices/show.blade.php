<x-app-layout title="Supplier invoice"><div class="mb-6 flex flex-wrap items-start justify-between gap-4"><x-page-header title="{{ $invoice->internal_number ?? 'Draft #'.$invoice->id }}" description="Supplier invoice {{ $invoice->supplier_invoice_number }} · {{ ucfirst(str_replace('_', ' ', $invoice->status->value)) }}" /><div class="flex gap-2">@can('update', $invoice)<a href="{{ route('supplier-invoices.edit', $invoice) }}" class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Edit</a>@endcan @can('print', $invoice)<a href="{{ route('supplier-invoices.print', $invoice) }}" class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Print</a>@endcan</div></div><section class="grid gap-4 rounded-2xl bg-white p-6 text-sm shadow-sm ring-1 ring-slate-200 md:grid-cols-3"><div><span class="text-slate-500">Supplier snapshot</span><p>{{ $invoice->supplier_name }}</p><p>{{ $invoice->supplier_tin ?: '—' }}</p></div><div><span class="text-slate-500">Invoice / due date</span><p>{{ $invoice->invoice_date->format('M d, Y') }} / {{ $invoice->due_date->format('M d, Y') }}</p></div><div><span class="text-slate-500">Fiscal period</span><p>{{ $invoice->fiscalPeriod->name }}</p></div></section><div class="mt-5 overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full text-sm"><thead><tr><th class="px-4 py-3 text-left">Description</th><th class="px-4 py-3 text-right">Qty</th><th class="px-4 py-3 text-right">Unit cost</th><th class="px-4 py-3 text-right">Gross</th><th class="px-4 py-3 text-right">Discount</th><th class="px-4 py-3 text-right">Net</th></tr></thead><tbody>@foreach($invoice->lines as $line)<tr><td class="px-4 py-3">{{ $line->description }}</td><td class="px-4 py-3 text-right">{{ $line->quantity }}</td><td class="px-4 py-3 text-right">{{ $line->unit_cost }}</td><td class="px-4 py-3 text-right">{{ $line->gross_amount }}</td><td class="px-4 py-3 text-right">{{ $line->discount_amount }}</td><td class="px-4 py-3 text-right">{{ $line->net_amount }}</td></tr>@endforeach</tbody></table></div><section class="mt-5 ml-auto grid max-w-xl grid-cols-2 gap-2 rounded-2xl bg-white p-6 text-sm shadow-sm ring-1 ring-slate-200"><span>Gross purchases</span><strong class="text-right">{{ $invoice->gross_purchase_amount }}</strong><span>Discounts</span><strong class="text-right">{{ $invoice->discount_amount }}</strong><span>Freight / other charges</span><strong class="text-right">{{ $invoice->freight_amount }} / {{ $invoice->other_charges_amount }}</strong><span>Expected withholding</span><strong class="text-right">({{ $invoice->withholding_expected_amount }})</strong><span>Total payable</span><strong class="text-right">{{ $invoice->total_payable }}</strong><span>Paid / balance</span><strong class="text-right">{{ $invoice->paid_amount }} / {{ $invoice->balance_due }}</strong></section><div class="mt-5 flex flex-wrap gap-3">@can('post', $invoice)<form method="POST" action="{{ route('supplier-invoices.transition', $invoice) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="posted"><button class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Post invoice</button></form>@endcan @can('void', $invoice)<form method="POST" action="{{ route('supplier-invoices.transition', $invoice) }}" class="flex gap-2">@csrf @method('PATCH')<input type="hidden" name="status" value="voided"><input name="reason" required placeholder="Void reason" class="rounded-lg border border-slate-300 px-3 py-2"><button class="rounded-lg border border-red-300 px-4 py-2 text-sm font-semibold text-red-700">Void</button></form>@endcan</div><x-purchasing-record-panel :record="$invoice" type="supplier_invoice" /></x-app-layout>

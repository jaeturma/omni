<x-app-layout title="Cash Receipts"><x-page-header title="Cash Receipts" description="Posted cash, bank, and e-wallet inflows."/><div class="mb-5 flex justify-end">@can('create', \App\Models\CashReceipt::class)<a href="{{ route('cash-receipts.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Record receipt</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white ring-1 ring-slate-200"><table class="min-w-full text-sm"><thead><tr><th class="px-5 py-3 text-left">Number</th><th>Date</th><th>Source</th><th>Account</th><th>Payer</th><th class="text-right">Net deposited</th><th>Status</th></tr></thead><tbody>@forelse($receipts as $receipt)<tr class="border-t"><td class="px-5 py-3"><a href="{{ route('cash-receipts.show', $receipt) }}" class="font-semibold text-blue-700">{{ $receipt->receipt_number ?? 'Draft #'.$receipt->id }}</a></td><td class="text-center">{{ $receipt->receipt_date->format('M d, Y') }}</td><td class="text-center">{{ str($receipt->source_type->value)->headline() }}</td><td class="text-center">{{ $receipt->financialAccount->code }}</td><td class="text-center">{{ $receipt->payer_name }}</td><td class="text-right">PHP {{ number_format((float)$receipt->net_amount_deposited, 2) }}</td><td class="text-center capitalize">{{ $receipt->status->value }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center">No cash receipts found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $receipts->links() }}</div></x-app-layout>

<x-app-layout :title="$label">
    <div class="flex flex-col gap-6">
        <div class="flex flex-wrap items-start justify-between gap-3"><div><h1 class="text-2xl font-semibold text-slate-900">{{ $label }}</h1><p class="mt-1 text-sm text-slate-600">{{ $business?->registered_business_name ?? 'Business identity missing' }} · {{ $classification }}</p></div><div class="flex gap-2"><a href="{{ route($group === 'books' ? 'books-of-accounts.print' : 'tax-schedules.print', $filters) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-medium">Print view</a>@can($group === 'books' ? 'books-of-accounts.export' : 'tax-schedules.export')<a href="{{ route($group === 'books' ? 'books-of-accounts.export' : 'tax-schedules.export', $filters) }}" class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-medium text-white">Export CSV</a>@endcan</div></div>
        @if($configuration_warning)<div class="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">{{ $configuration_warning }}</div>@endif
        @if($balances)<div class="grid gap-4 sm:grid-cols-2"><div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><div class="text-xs uppercase text-slate-500">Beginning balance</div><div class="text-xl font-semibold">₱{{ number_format((float)$balances['beginning'], 4) }}</div></div><div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><div class="text-xs uppercase text-slate-500">Ending balance</div><div class="text-xl font-semibold">₱{{ number_format((float)$balances['ending'], 4) }}</div></div></div>@endif
        <div class="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm text-blue-900">{{ $disclaimer }}</div>
        <form method="GET" class="grid gap-3 rounded-2xl bg-white p-5 ring-1 ring-slate-200 sm:grid-cols-2 lg:grid-cols-5"><label class="grid gap-1 text-sm">Report<select name="report" class="rounded-lg border-slate-300">@foreach($reports as $key=>$definition) @if($definition['group']===$group)<option value="{{ $key }}" @selected($report===$key)>{{ $definition['label'] }}</option>@endif @endforeach</select></label><label class="grid gap-1 text-sm">Start<input type="date" name="start_date" value="{{ $filters['start_date'] }}" class="rounded-lg border-slate-300"></label><label class="grid gap-1 text-sm">End<input type="date" name="end_date" value="{{ $filters['end_date'] }}" class="rounded-lg border-slate-300"></label><label class="grid gap-1 text-sm">Fiscal year<select name="fiscal_year_id" class="rounded-lg border-slate-300"><option value="">Custom range</option>@foreach($fiscalYears as $year)<option value="{{ $year->id }}" @selected(($filters['fiscal_year_id']??null)==$year->id)>{{ $year->name }}</option>@endforeach</select></label><label class="grid gap-1 text-sm">Tax period<select name="tax_period_id" class="rounded-lg border-slate-300"><option value="">None</option>@foreach($taxPeriods as $period)<option value="{{ $period->id }}" @selected(($filters['tax_period_id']??null)==$period->id)>{{ $period->label }}</option>@endforeach</select></label><div class="lg:col-span-5"><button class="rounded-lg bg-blue-700 px-4 py-2 font-medium text-white">Generate</button></div></form>
        <div class="overflow-hidden rounded-2xl bg-white ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr>@foreach($headers as $header)<th class="px-4 py-3 text-left">{{ $header }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($rows as $row)<tr>@foreach($row as $value)<td class="whitespace-nowrap px-4 py-3">{{ $value }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($headers) }}" class="px-4 py-10 text-center text-slate-500">No posted records in the selected period.</td></tr>@endforelse</tbody>@if($totals!==[])<tfoot class="bg-slate-50"><tr><th class="px-4 py-3 text-left">Totals</th><td colspan="{{ max(count($headers)-1,1) }}" class="px-4 py-3">@foreach($totals as $name=>$value)<span class="mr-5">{{ $name }}: {{ $value }}</span>@endforeach</td></tr></tfoot>@endif</table></div></div>
        <p class="text-xs text-slate-500">Generated by {{ $generated_by }} at {{ $generated_at->format('Y-m-d H:i:s T') }} · Parameters: {{ $filters['start_date'] }} to {{ $filters['end_date'] }}</p>
    </div>
</x-app-layout>

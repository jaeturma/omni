<x-app-layout title="Operating expenses"><div class="mb-6 flex flex-wrap items-start justify-between gap-4"><x-page-header title="Operating expenses" description="Business costs recorded outside purchase-order and supplier-invoice workflows." />@can('create', \App\Models\Expense::class)<a href="{{ route('expenses.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">New expense</a>@endcan</div><form method="GET" class="mb-5 flex flex-wrap items-end gap-3"><label class="flex flex-col gap-1 text-sm font-medium">Search<input name="search" value="{{ request('search') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Status<select name="status" class="rounded-lg border border-slate-300 px-3 py-2"><option value="">All</option>@foreach(\App\Enums\ExpenseStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select></label><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Filter</button></form><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead><tr><th class="px-5 py-3 text-left">Number</th><th class="px-5 py-3 text-left">Date / Payee</th><th class="px-5 py-3 text-left">Category</th><th class="px-5 py-3 text-right">Gross / Cash</th><th class="px-5 py-3 text-left">Status</th></tr></thead><tbody>@forelse($expenses as $expense)<tr><td class="px-5 py-4"><a href="{{ route('expenses.show', $expense) }}" class="font-semibold text-blue-700">{{ $expense->expense_number ?? 'Draft #'.$expense->id }}</a></td><td class="px-5 py-4">{{ $expense->expense_date->format('M d, Y') }}<p class="text-xs text-slate-500">{{ $expense->payee_name }}</p></td><td class="px-5 py-4">{{ str_replace('_', ' ', $expense->expense_category) }}</td><td class="px-5 py-4 text-right">{{ $expense->gross_amount }} / {{ $expense->net_cash_paid }}</td><td class="px-5 py-4 capitalize">{{ $expense->status->value }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-8 text-center text-slate-500">No expenses found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $expenses->links() }}</div></x-app-layout>

<!doctype html><html lang="en"><head><meta charset="utf-8"><title>{{ $expense->expense_number ?? 'Draft expense' }}</title></head><body><h1>Expense Voucher {{ $expense->expense_number ?? 'Draft' }}</h1><p>{{ $expense->expense_date->toDateString() }} · {{ $expense->payee_name }} · {{ $expense->expense_category }}</p><p>{{ $expense->description }}</p><p>Business purpose: {{ $expense->business_purpose }}</p><p>Gross {{ $expense->gross_amount }} · Withholding {{ $expense->withholding_amount }} · Other deductions {{ $expense->other_deductions }} · Cash paid {{ $expense->net_cash_paid }}</p><p>Receipt/invoice: {{ $expense->receipt_available ? ($expense->receipt_reference ?: 'Available') : 'Not available' }}</p></body></html>

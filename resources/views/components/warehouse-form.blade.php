@props(['action', 'method' => 'POST', 'warehouse' => null, 'submitLabel'])
<form method="POST" action="{{ $action }}" class="max-w-3xl rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">@csrf @if ($method !== 'POST') @method($method) @endif
<div class="grid gap-4 md:grid-cols-2"><label class="flex flex-col gap-1 text-sm font-medium">Warehouse code<input name="code" value="{{ old('code', $warehouse?->code) }}" required maxlength="30" class="rounded-lg border border-slate-300 px-3 py-2">@error('code')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium">Warehouse name<input name="name" value="{{ old('name', $warehouse?->name) }}" required class="rounded-lg border border-slate-300 px-3 py-2">@error('name')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium md:col-span-2">Location address<textarea name="address" rows="3" required class="rounded-lg border border-slate-300 px-3 py-2">{{ old('address', $warehouse?->address) }}</textarea>@error('address')<span class="text-red-700">{{ $message }}</span>@enderror</label><label class="flex flex-col gap-1 text-sm font-medium">Status<select name="status" class="rounded-lg border border-slate-300 px-3 py-2"><option value="active" @selected(old('status', $warehouse?->status ?? 'active') === 'active')>Active</option><option value="inactive" @selected(old('status', $warehouse?->status) === 'inactive')>Inactive</option></select>@error('status')<span class="text-red-700">{{ $message }}</span>@enderror</label></div>
<div class="mt-5 flex gap-3"><button class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">{{ $submitLabel }}</button><a href="{{ route('warehouses.index') }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Cancel</a></div></form>

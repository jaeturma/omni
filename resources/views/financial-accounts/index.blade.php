<x-app-layout title="Financial Accounts"><x-page-header title="Financial Accounts" description="Maintain cash, bank, e-wallet, petty-cash, and clearing account masters." /><div class="mb-5 flex flex-wrap items-end justify-between gap-4"><form method="GET" class="flex flex-wrap items-end gap-3"><label class="grid gap-1 text-sm">Search<input name="search" value="{{ request('search') }}" class="rounded-lg border-slate-300"></label><label class="grid gap-1 text-sm">Type<select name="type" class="rounded-lg border-slate-300"><option value="">All types</option>@foreach(\App\Enums\FinancialAccountType::cases() as $type)<option value="{{ $type->value }}" @selected(request('type')===$type->value)>{{ str($type->value)->replace('_',' ')->title() }}</option>@endforeach</select></label><label class="grid gap-1 text-sm">Status<select name="status" class="rounded-lg border-slate-300"><option value="">All</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select></label><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Filter</button></form>@can('create',\App\Models\FinancialAccount::class)<a href="{{ route('financial-accounts.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create account</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white ring-1 ring-slate-200"><table class="min-w-full text-sm"><thead><tr><th class="px-5 py-3 text-left">Code</th><th>Account</th><th>Type</th><th>Bank</th><th>Number</th><th>Opening balance</th><th>Status</th></tr></thead><tbody>@forelse($accounts as $account)<tr class="border-t text-center"><td class="px-5 py-3 text-left font-mono"><a class="text-blue-700" href="{{ route('financial-accounts.show',$account) }}">{{ $account->code }}</a></td><td>{{ $account->name }}</td><td>{{ str($account->type->value)->replace('_',' ')->title() }}</td><td>{{ $account->bank?->name ?? '—' }}</td><td class="font-mono">{{ $account->maskedAccountNumber() }}</td><td>₱{{ number_format((float)$account->opening_balance,2) }}</td><td>{{ $account->active ? 'Active' : 'Inactive' }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center">No financial accounts found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $accounts->links() }}</div></x-app-layout>

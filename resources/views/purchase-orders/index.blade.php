<x-app-layout title="Purchase orders"><x-page-header title="Purchase orders" description="Manage direct and request-based supplier orders." /><div class="mb-5 flex flex-wrap items-end justify-between gap-4"><form method="GET" class="flex flex-wrap items-end gap-3"><label class="flex flex-col gap-1 text-sm font-medium">Search<input name="search" value="{{ request('search') }}" class="rounded-lg border border-slate-300 px-3 py-2"></label><label class="flex flex-col gap-1 text-sm font-medium">Status<select name="status" class="rounded-lg border border-slate-300 px-3 py-2"><option value="">All</option>@foreach(\App\Enums\PurchaseOrderStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ ucfirst(str_replace('_', ' ', $status->value)) }}</option>@endforeach</select></label><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold">Filter</button></form>@can('create', \App\Models\PurchaseOrder::class)<a href="{{ route('purchase-orders.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create direct order</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left"><tr><th class="px-6 py-3">Number</th><th class="px-6 py-3">Date</th><th class="px-6 py-3">Supplier</th><th class="px-6 py-3 text-right">Total</th><th class="px-6 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($purchaseOrders as $order)<tr><td class="px-6 py-4"><a href="{{ route('purchase-orders.show', $order) }}" class="font-semibold text-blue-700">{{ $order->purchase_order_number ?? 'Draft #'.$order->id }}</a></td><td class="px-6 py-4">{{ $order->order_date->format('M d, Y') }}</td><td class="px-6 py-4">{{ $order->supplier_name }}</td><td class="px-6 py-4 text-right">₱{{ number_format((float)$order->grand_total, 2) }}</td><td class="px-6 py-4 capitalize">{{ str_replace('_', ' ', $order->status->value) }}</td></tr>@empty<tr><td colspan="5" class="px-6 py-8 text-center text-slate-500">No purchase orders found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $purchaseOrders->links() }}</div></x-app-layout>

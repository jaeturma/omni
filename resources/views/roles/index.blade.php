<x-app-layout title="Role Matrix"><x-page-header title="Role-Permission Matrix" description="Read-only overview of access granted by each initial role." />
<div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr><th class="sticky left-0 bg-slate-50 px-4 py-3 text-left">Permission</th>@foreach ($roles as $role)<th class="px-4 py-3 text-center">{{ $role->name }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@foreach ($permissions as $permission)<tr><td class="sticky left-0 bg-white px-4 py-3 font-mono">{{ $permission->name }}</td>@foreach ($roles as $role)<td class="px-4 py-3 text-center">{{ $role->permissions->contains('id', $permission->id) ? 'Yes' : '—' }}</td>@endforeach</tr>@endforeach</tbody></table></div></x-app-layout>

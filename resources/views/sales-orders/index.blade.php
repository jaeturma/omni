<x-app-layout title="Sales Orders"><x-page-header title="Sales Orders" description="Manage direct and quotation-based customer orders." /><div class="mb-5 flex justify-end">@can('create',\App\Models\SalesOrder::class)<a href="{{ route('sales-orders.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Create sales order</a>@endcan</div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead><tr><th class="px-5 py-3 text-left">Number</th><th class="px-5 py-3 text-left">Customer</th><th class="px-5 py-3">Date</th><th class="px-5 py-3">Total</th><th class="px-5 py-3">Status</th></tr></thead><tbody>@forelse($salesOrders as $order)<tr><td class="px-5 py-3"><a class="font-semibold text-blue-700" href="{{ route('sales-orders.show',$order) }}">{{ $order->sales_order_number ?? 'Draft #'.$order->id }}</a></td><td class="px-5 py-3">{{ $order->customer_name }}</td><td class="px-5 py-3">{{ $order->order_date->format('M d, Y') }}</td><td class="px-5 py-3 text-right">₱{{ number_format((float)$order->grand_total,2) }}</td><td class="px-5 py-3 capitalize">{{ str_replace('_',' ',$order->status->value) }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-8 text-center">No sales orders found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $salesOrders->links() }}</div></x-app-layout>

<!DOCTYPE html><html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Reset password | {{ config('app.name') }}</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-100 text-slate-900 antialiased"><main class="flex min-h-screen items-center justify-center px-4 py-12"><section class="w-full max-w-md rounded-2xl bg-white p-8 shadow-sm ring-1 ring-slate-200"><h1 class="text-2xl font-bold">Reset password</h1><p class="mt-2 text-sm text-slate-600">Enter your account email to request a secure reset link.</p>
@if (session('status'))<p class="mt-4 rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('status') }}</p>@endif
<form method="POST" action="{{ route('password.email') }}" class="mt-6 flex flex-col gap-4">@csrf<label class="flex flex-col gap-1 text-sm font-medium">Email address<input type="email" name="email" value="{{ old('email') }}" required autofocus class="rounded-lg border border-slate-300 px-3 py-2.5">@error('email')<span class="text-red-700">{{ $message }}</span>@enderror</label><button class="rounded-lg bg-blue-700 px-4 py-2.5 text-sm font-semibold text-white">Send reset link</button><a href="{{ route('login') }}" class="text-center text-sm font-semibold text-blue-700">Back to sign in</a></form></section></main></body></html>
